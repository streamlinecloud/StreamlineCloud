# StreamlineCloud Backend
The backend for StreamlineCloud. Each setup can only have one backend.
The backend is the communication layer between the nodes, the Minecraft servers and 3rd party modules like the Dashboard.