# StreamlineCloud Launcher
An optional module that allows the user to start StreamlineCloud with one single command.
It automatically sets up the broker, a first node and checks for updates.
Professional and complex setups with multiple nodes may not need the launcher.